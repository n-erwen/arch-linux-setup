# arch-linux-setup-example

## How to run arch-linux-vmware-install.sh

The VM created must have its firmware type set to UEFI.
After creating VM in VMWare Workstation Pro and booting, run the following commands:

```sh
curl -L https://tinyurl.com/mryduvde > install.sh
chmod +x install.sh
./install.sh
```
